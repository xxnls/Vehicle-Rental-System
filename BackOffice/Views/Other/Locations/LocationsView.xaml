<UserControl x:Class="BackOffice.Views.Other.Locations.LocationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BackOffice.Views.Other.Locations"
             xmlns:locations="clr-namespace:BackOffice.ViewModels.Other.Locations"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <!-- Bind to the ViewModel -->
        <locations:LocationsViewModel />
    </UserControl.DataContext>

    <Grid>
        <DataGrid ItemsSource="{Binding Locations}" AutoGenerateColumns="True" 
                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <!--<Button Content="Load Locations" Command="{Binding LoadLocationsCommand}" Width="100" Height="50"/>-->
    </Grid>
</UserControl>
